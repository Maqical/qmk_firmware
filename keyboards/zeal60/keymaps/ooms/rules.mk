RGBLIGHT_ENABLE = yes       # Underglow light
AUTO_SHIFT_ENABLE = no      # Tap a key and you get its character. Tap a key, but hold it slightly longer and you get its shifted state.
EXTRAFLAGS        += -flto    # gcc-avr flags 